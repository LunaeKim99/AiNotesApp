class AppConstants {
  AppConstants._();

  static const String appName = 'Note App';
  static const double mobileBreakpoint = 600;
  static const double tabletBreakpoint = 1024;
  static const double sidebarWidthDesktop = 280;
  static const double sidebarWidthTablet = 200;
  static const double sidebarCollapsedWidth = 0;
  static const Duration sidebarAnimationDuration = Duration(milliseconds: 250);
}
